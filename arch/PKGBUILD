# Maintainer: George Raven <GeorgeRavenCommunity AT outlook dot com>
pkgname=nemesyst-git
pkgsrcname="nemesyst"
pkgver=0.9.r294
pkgrel=1
pkgdesc="Generalised, sequence-based, deep-learning framework of the gods. Warning may include GANs, does not include nuts."
arch=('x86_64')

# nemesyst source code url
url="https://github.com/DreamingRaven/Nemesyst"
# nemesyst helper lib will be made into its own package later
urllib="https://github.com/DreamingRaven/RavenPythonLib"

license=('MIT') # MIT is a special case store a copy in /usr/share/pkgname
groups=("nemesyst-base")
depends=('mongodb' 'python' 'python-pymongo' 'python-keras' 'python-pandas' 'python-numpy' 'python-h5py')
makedepends=() # 'bzr', 'git', 'mercurial' or 'subversion'
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
source=('nemesyst::git+https://github.com/DreamingRaven/Nemesyst#branch=master')
noextract=()
md5sums=('SKIP')

pkgver() {
	cd "$srcdir/${pkgsrcname}"
	printf "%s" "$(git describe --long | sed 's/\([^-]*-\)g/r\1/;s/-/./g')"
}

prepare() {
	cd "$srcdir/${pkgsrcname}"
	git checkout master # get off of makepkg branch
	git clone ${urllib} # add static lib in sub dir TODO make its own package
}

build() {
	cd "$srcdir/${pkgsrcname}"
}

check() {
	cd "$srcdir/${pkgsrcname}"
	# check that program can run while self checking and updating if somethings missing
	python3 nemesyst.py --toUpdate
}

package() {
	cd "$srcdir/${pkgsrcname}"
}
